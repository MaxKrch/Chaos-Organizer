import BaseComponent from '../helpers/BaseComponent';
import ContextMenu from './popups/ContextMenu';
import Modal from './popups/Modal';

export default class FileNote extends BaseComponent {
	constructor(container) {
		super(container);

	}
}